# Required for commands that access Google Cloud.
GCP_PROJECT_ID=your-gcp-project-id

# Seoul is the default region for this project.
GCP_REGION=asia-northeast3

# Resource names may be overridden once infrastructure is provisioned.
BIGQUERY_DATASET=career_radar
GCS_BUCKET=
GEMINI_MODEL=gemini-2.5-flash

